<div class="col-sm">
  <%= form_for(@user) do |f| %>

    <div class="form-group">
      <%= f.label :name %><br />
      <%= f.text_field :name, class: "form-control" %>
    </div>

    
    <%= f.submit class: "btn btn-primary"%>

  <% end %>
</div>
